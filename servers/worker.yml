- hosts: localhost
  connection: local
  gather_facts: false
  vars:
    instance_type: t3.small
    groups:
      - all
      - workers
  roles:
  - ec2

- hosts: workers
  become: true
  roles:
  - base
  - ntpd
  - devtoolset
  - nodejs
  - libreoffice
  - app
  - clamav
