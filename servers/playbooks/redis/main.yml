- name: Extract nodejs
  unarchive:
    src: http://download.redis.io/releases/redis-4.0.0.tar.gz
    dest: /home/centos
    remote_src: yes
    
- name: Build nodejs
  shell: source scl_source enable devtoolset-7 && ./configure && make && make install
  args:
    chdir: /home/centos/node-v8.14.0
