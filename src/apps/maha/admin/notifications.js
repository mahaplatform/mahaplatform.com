import { Notifications } from 'maha'

const notifications = new Notifications([
  {
    code: 'item_comment',
    title: 'Item Comment',
    description: 'someone comments on an item I own'
  }
])

export default notifications
