import ChatBadge from './chat'
import Chatbar from '../components/chats/chatbar'

const badges = [
  { weight: 8, sidebar: Chatbar, route: '/admin/chat', component: ChatBadge, tooltip: 'Chat' }
]

export default badges
