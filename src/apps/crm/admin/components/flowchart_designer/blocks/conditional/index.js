import Token from './token'
import Form from './form'

export default {
  icon: 'random',
  label: 'If / Then',
  type: 'conditional',
  action: 'ifelse',
  config: {},
  form: Form,
  token: Token
}
