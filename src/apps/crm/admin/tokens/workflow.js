import React from 'react'

const WorkflowToken = ({ title }) => {
  return (
    <div className="type-token">
      <strong>{ title }</strong><br />
      No Trigger
    </div>
  )
}

export default WorkflowToken
