<!DOCTYPE html>
<html lang="en-us">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <link rel="stylesheet" type="text/css" href="/admin/css/semantic.min.css">
    <link rel="stylesheet" type="text/css" href="/admin/css/font-awesome.min.css">
    <style>
      body {
        background-color:#999999;
      }
      main {
        width:1200px;
        max-width:94%;
        margin:0 auto;
        background-color:#FFFFFF;
        box-shadow:3px 3px 25px -5px #333333;
        height:100%;
        position:relative;
      }
      .wrapper {
        padding-top:50px;
        display: flex;
      }
      .wrapper, iframe {
        border:none;
        width:100%;
        height:100%;
        min-width: 100%;
      }
      .wrapper iframe {
        flex: 0;
        margin: auto;
      }
      .wrapper p, .wrapper img, .wrapper video {
        margin: auto;
      }
      .header-bar {
        width:100%;
        position:absolute;
        top:0;
        left:0;
        background:rgba(255,255,255,.8);
        padding:15px 3%;
      }
      .btn {
        float: right;
      }
      .btn a {
        background-color: #2185d0;
        color: #FFFFFF;
        padding: 12px 18px;
        border-radius: 5px;
      }
    </style>
    <title><%= asset.url %></title>
  </head>
  <body>
    <main>
      <div class="wrapper">
        <% if ((asset.content_type.match(/pdf/)) || (asset.content_type.match(/excel/)) || (asset.content_type.match(/spreadsheetml/)) || (asset.content_type.match(/msword/)) || (asset.content_type.match(/wordprocessingml/)) || (asset.content_type.match(/powerpoint/)) || (asset.content_type.match(/presentationml/))) { %>
          <iframe src="https://docs.google.com/gview?url=<%= asset.url %>&embedded=true"></iframe>
        <% } else if(asset.content_type.match(/image/)) { %>
          <img src="<%= asset.url %>" alt="<%= asset.original_file_name %>" />
        <% } else if(asset.content_type.match(/video/)) { %>
          <video width="500" height="350" controls>
            <source src="<%= asset.url %>" type="<%= asset.content_type %>">
            Preview unavailable, <a href="<%= asset.url %>">click here to download the file.</a>
          </video>
        <% } else if(asset.content_type.match(/audio/)) { %>
          <iframe src="<%= asset.url %>"></iframe>
        <% } else { %>
          <p>Preview unavailable, <a href="<%= asset.url %>">click here to download the file.</a></p>
        <% } %>
      </div>
      <div class="header-bar">
        <div class="text">
          Now viewing: <i class="fa fa-fw fa-file-o"></i> <%= asset.original_file_name %>
        </di>
        <div class="btn">
          <a href="<%= asset.url %>">
            <i class="fa fa-download"></i> Download File
          </a>
        </div>
      </div>
    </main>
  </body>
</html>
