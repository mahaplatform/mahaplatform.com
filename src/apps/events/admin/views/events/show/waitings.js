import PropTypes from 'prop-types'
import React from 'react'

const Waitings = ({ waitings }) => {

  return <div>waitings</div>

}

Waitings.propTypes = {
  waitings: PropTypes.array
}

export default Waitings
