local:
  hosts:
    localhost
all:
  vars:
    ansible_become: true
    ansible_become_method: su
    ansible_become_exe: 'sudo su -'
    ansible_ssh_private_key_file: ~/.ssh/mahaplatform
    ansible_user: centos
