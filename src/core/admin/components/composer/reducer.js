export const INITIAL_STATE = {
  emojis: false,
  link: null
}

const reducer = (state = INITIAL_STATE, action) => {

  switch (action.type) {

  default:
    return state

  }

}

export default reducer
