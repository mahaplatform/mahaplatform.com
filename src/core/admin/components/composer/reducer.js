export const INITIAL_STATE = {
  attachments: [],
  link: null
}

const reducer = (state = INITIAL_STATE, action) => {

  switch (action.type) {

  default:
    return state

  }

}

export default reducer
